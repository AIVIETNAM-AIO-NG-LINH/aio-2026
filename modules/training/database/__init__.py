"""Tầng database của module Training (kiểu Laravel `database/`).

Chứa `migrations/` — Django được trỏ tới đây qua `MIGRATION_MODULES` trong
config/settings.py (mặc định Django tìm migrations ở `<app>/migrations/`).
"""
