"""Tầng database của module Example (kiểu Laravel `database/`).

Chứa `migrations/` (gồm `0001_initial` tạo bảng `example`) — Django được trỏ tới
đây qua `MIGRATION_MODULES` trong config/settings.py (mặc định Django tìm
migrations ở `<app>/migrations/`).
"""
