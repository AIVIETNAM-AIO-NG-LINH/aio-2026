"""Tầng database của module Chatbot (kiểu Laravel `database/`).

Hiện chứa `migrations/` — Django được trỏ tới đây qua `MIGRATION_MODULES` trong
config/settings.py (mặc định Django tìm migrations ở `<app>/migrations/`).
"""
